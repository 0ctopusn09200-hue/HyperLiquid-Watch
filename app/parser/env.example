# ==========================================
# Hyperliquid Configuration
# ==========================================
HL_COIN=BTC

# ==========================================
# Feature Flags (Default: All Disabled)
# ==========================================
# Enable liquidation event subscription (requires MONITORED_USERS)
ENABLE_LIQUIDATION_FEED=false

# Enable position updates subscription (requires MONITORED_USERS)
ENABLE_POSITION_FEED=false

# Enable real-time price feed subscription
ENABLE_PRICE_FEED=true

# ==========================================
# User Monitoring (Required for liquidation/position feeds)
# ==========================================
# Comma-separated wallet addresses to monitor
# Example: 0x123...,0x456...
# Leave empty to monitor none (liquidation/position feeds will be skipped)
MONITORED_USERS=

# ==========================================
# Kafka Configuration
# ==========================================
KAFKA_BOOTSTRAP_SERVERS=kafka:9092
# For docker-compose network, use: kafka:9092

# Default topic for trades (existing behavior, DO NOT CHANGE)
KAFKA_TOPIC=hyperliquid-raw-trades

# New topics for extended feeds (only used when feature flags enabled)
# Naming convention aligned: singular form (except positions which remains plural)
# For backward compatibility, can override via environment variables to use plural forms
KAFKA_TOPIC_LIQUIDATIONS=hyperliquid-raw-liquidation
KAFKA_TOPIC_POSITIONS=hyperliquid-raw-positions
KAFKA_TOPIC_PRICES=hyperliquid-raw-price

# ==========================================
# Run Mode
# ==========================================
# Set to 1 to only print messages without sending to Kafka
DRY_RUN=0
